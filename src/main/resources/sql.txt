create database mycloud character set utf8;

create table file(
filename char(255),
date date,
description char(255),
fileLength bigint not null,
fileSha256 char(64),
fileBytes mediumBlob not null,
primary key(filename, date),
index (description),
index (fileSha256)
);